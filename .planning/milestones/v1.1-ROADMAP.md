# Milestone v1.1: 95th Percentile Utilization Metrics

**Status:** ✅ SHIPPED 2026-08-08
**Phases:** 3-5
**Total Plans:** 5

## Overview

Added optional per-edge 95th percentile bandwidth utilization metrics (tx, rx, total) across configurable month ranges. Users invoke `--collect_95th --months N` to collect metrics via the VCO `metrics/getEdgeLinkSeries` API, compute daily and monthly 95th percentile values, and output per-month CSVs in a timestamped zip archive.

## Phases

### Phase 3: CLI & Configuration

**Goal**: Users can invoke the script with new flags, and existing behavior is unchanged when flags are absent
**Depends on**: Phase 2 (v1.0 complete)
**Plans**: 1 plan

Plans:

- [x] 03-01-PLAN.md — Add argparse CLI with --collect_95th and --months flags, plus automated tests

**Requirements**: CLI-01, CLI-02, CLI-03, CLI-04
**Success Criteria**:
  1. User can run `vco_edge_export.py --collect_95th` and the script recognizes and accepts the flag without error
  2. User can run `vco_edge_export.py --months 3` to specify N months; omitting the flag defaults to 1 month
  3. Running without `--collect_95th` produces identical CSV output to existing v1.0 behavior
  4. Running with `--help` displays the new flags with descriptions

### Phase 4: Metrics Collection & Calculation

**Goal**: Per-edge 95th percentile bandwidth values (tx, rx, total) are accurately computed from VCO API data
**Depends on**: Phase 3
**Plans**: 2 plans

Plans:
**Wave 1**
- [x] 04-01-PLAN.md — Metrics computation module (TDD): month ranges, bytes-to-Mbps, percentile, link aggregation, edge-month pipeline

**Wave 2** *(blocked on Wave 1 completion)*
- [x] 04-02-PLAN.md — API integration: getEdgeLinkSeries wrapper and metrics collection orchestration in __main__

**Requirements**: METR-01, METR-02, METR-03, METR-04, CALC-01, CALC-02, CALC-03
**Success Criteria**:
  1. Script calls `metrics/getEdgeLinkSeries` with bytesTx and bytesRx for each edge over each target month using millisecond timestamps and the edge `id` field
  2. Target months are the last N complete calendar months — the current partial month is never included
  3. Aggregated bytes across all edge links per 5-minute sample are converted to Mbps (bytes * 8 / 1048576 / 300)
  4. Daily 95th percentile is the value at ceil(count * 0.95) in the sorted sample list, computed independently for tx, rx, and total
  5. Monthly 95th percentile is derived from daily values using the same ceil(count * 0.95) logic, accounting for variable days per month (28-31)

### Phase 5: Output & Packaging

**Goal**: Users receive a single zip archive containing correctly named per-month CSV files with 95th percentile columns
**Depends on**: Phase 4
**Plans**: 2 plans

Plans:
**Wave 1**
- [x] 05-01-PLAN.md — Output module (TDD): extract_vco_name, write_month_csvs, create_zip_archive with full test coverage

**Wave 2** *(blocked on Wave 1 completion)*
- [x] 05-02-PLAN.md — Wire output module into __main__ block with timestamped temp dir, zip packaging, and cleanup

**Requirements**: OUT-01, OUT-02, OUT-03, OUT-04
**Success Criteria**:
  1. Running with `--collect_95th` produces a zip archive in the working directory upon completion
  2. Each target month generates a separate CSV file named `vco_name.MM-YYYY.csv`
  3. Each CSV contains 4 additional columns: Month-Year, monthly_tx_95th_mbps, monthly_rx_95th_mbps, monthly_total_95th_mbps
  4. All CSVs are stored in a timestamped temp directory before being compressed into the zip

---

## Milestone Summary

**Key Decisions:**

- Used pure functions (stdlib only) for metrics computation — no new dependencies for core math
- TDD red-green cycle for all new modules (metrics.py, output.py)
- Left merge for metrics enrichment — unmatched edges get NaN automatically
- tempfile.mkdtemp with try/finally cleanup for secure temp directory handling
- Conditional pipeline — metrics collection only runs when --collect_95th is set

**Issues Resolved:**

- Hardcoded epoch constants in tests corrected against Python datetime
- Tautological wiring tests replaced with real invocations (code review fix)
- print() replaced with logging in api_call (code review fix)
- create_zip_archive filtered to files only (code review fix)
- extract_vco_name raises ValueError for scheme-less URLs (code review fix)
- edge_info_list deduplication before metrics pipeline (code review fix)

**Issues Deferred:**

- None

**Technical Debt Incurred:**

- Requirements checkboxes in REQUIREMENTS.md not updated during phase execution (validated via SUMMARY.md instead)

---

_For current project status, see .planning/ROADMAP.md_
