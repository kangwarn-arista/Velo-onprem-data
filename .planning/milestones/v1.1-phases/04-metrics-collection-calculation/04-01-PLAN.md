---
phase: 04-metrics-collection-calculation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - metrics.py
  - tests/test_metrics.py
autonomous: true
requirements:
  - METR-02
  - METR-03
  - METR-04
  - CALC-01
  - CALC-02

must_haves:
  truths:
    - "get_target_months returns last N complete calendar months, never including the current partial month"
    - "bytes_to_mbps converts using the exact formula: bytes * 8 / 1_048_576 / 300"
    - "percentile_95 picks the value at position ceil(count * 0.95) in the sorted list (1-indexed)"
    - "aggregate_link_samples sums bytesTx and bytesRx across all links at each 5-minute sample index"
    - "compute_edge_month_metrics returns monthly_tx_95th_mbps, monthly_rx_95th_mbps, monthly_total_95th_mbps"
  artifacts:
    - path: "metrics.py"
      provides: "Pure computation functions for bandwidth metrics"
      exports: ["get_target_months", "bytes_to_mbps", "percentile_95", "aggregate_link_samples", "compute_edge_month_metrics"]
    - path: "tests/test_metrics.py"
      provides: "Test coverage for all metrics functions"
      min_lines: 100
  key_links:
    - from: "tests/test_metrics.py"
      to: "metrics.py"
      via: "import"
      pattern: "from metrics import"
---

<objective>
Create a metrics computation module with pure functions for month range calculation, bytes-to-Mbps conversion, 95th percentile computation, link-sample aggregation, and the full edge-month metrics pipeline.

Purpose: Isolate all mathematical and data transformation logic into a testable module that Plan 04-02 will wire into the main script via API calls.
Output: metrics.py with 5 exported functions, tests/test_metrics.py with comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-configuration/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core computation functions -- month ranges, bytes-to-Mbps, percentile</name>
  <files>metrics.py, tests/test_metrics.py</files>
  <read_first>
    - vco_edge_export.py (existing function docstring style with Google-style docstrings, module layout pattern, import organization)
    - tests/test_cli.py (existing test patterns: env var setup via os.environ.setdefault before import, pytest conventions, assertion style)
    - .planning/REQUIREMENTS.md (METR-02 month range, METR-04 bytes formula, CALC-01 percentile method)
  </read_first>
  <behavior>
    - get_target_months(1, date(2026, 8, 5)) returns a list with exactly 1 dict for July 2026: start_ms corresponds to 2026-07-01T00:00:00 UTC, end_ms corresponds to 2026-08-01T00:00:00 UTC, label is "07-2026"
    - get_target_months(3, date(2026, 8, 5)) returns 3 dicts in chronological order (oldest first): May 2026, June 2026, July 2026
    - get_target_months(2, date(2026, 1, 15)) crosses year boundary: returns Nov 2025, Dec 2025
    - get_target_months(1, date(2026, 8, 1)) returns July 2026 (current month always excluded, even on the 1st)
    - All start_ms and end_ms values are 13-digit integers (milliseconds since epoch)
    - Each dict also contains year (int) and month (int) keys
    - bytes_to_mbps(0) returns 0.0
    - bytes_to_mbps(1_048_576) returns approximately 0.02667 (exactly 8/300)
    - bytes_to_mbps(39_321_600) returns exactly 1.0
    - percentile_95(list(range(1, 101))) returns 95 (ceil(100*0.95) = 95th position)
    - percentile_95(list(range(1, 289))) returns 274 (ceil(288*0.95) = 274th position)
    - percentile_95([42]) returns 42 (ceil(1*0.95) = 1st position)
    - percentile_95([10, 20]) returns 20 (ceil(2*0.95) = 2nd position)
    - percentile_95([]) raises ValueError
  </behavior>
  <action>
    RED: Create tests/test_metrics.py. Do NOT set env vars or import vco_edge_export -- metrics.py is independent. Write test functions covering every behavior listed above. Import get_target_months, bytes_to_mbps, percentile_95 from metrics. Use from datetime import date for reference_date args. Use pytest.raises for the empty-list ValueError test. Use pytest.approx for floating-point comparisons (bytes_to_mbps(1_048_576) == pytest.approx(8/300)). Run tests -- they must fail with ImportError since metrics.py does not exist.

    GREEN: Create metrics.py at the repo root. Import only stdlib: math (ceil), calendar (monthrange -- not strictly needed but useful), datetime (date, datetime, timezone).

    Function 1 -- get_target_months(num_months: int, reference_date: date | None = None) -> list[dict]:
    If reference_date is None, use date.today(). Starting from the month immediately before reference_date, walk backward num_months times. For each month: compute start_ms as int(datetime(year, month, 1, tzinfo=timezone.utc).timestamp() * 1000). Compute end_ms as start of the next month (handle December->January year rollover). Build dict with keys: year, month, start_ms, end_ms, label (formatted as f"{month:02d}-{year}"). Reverse the collected list before returning so oldest month is first.

    Function 2 -- bytes_to_mbps(byte_count: int | float) -> float:
    Single expression: return byte_count * 8 / 1_048_576 / 300

    Function 3 -- percentile_95(values: list[float | int]) -> float | int:
    If not values, raise ValueError("Cannot compute percentile of empty list"). Sort a copy: sorted_vals = sorted(values). Compute position = math.ceil(len(sorted_vals) * 0.95). Return sorted_vals[position - 1].

    All three functions must have Google-style docstrings and type hints on parameters and return.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metrics.py -v -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - metrics.py exists at repo root with exactly 3 functions defined: get_target_months, bytes_to_mbps, percentile_95
    - tests/test_metrics.py exists with at least 10 test functions
    - uv run pytest tests/test_metrics.py -v exits with code 0
    - uv run python -c "from metrics import get_target_months; from datetime import date; m = get_target_months(1, date(2026, 8, 5)); assert m[0]['label'] == '07-2026'" exits 0
    - uv run python -c "from metrics import bytes_to_mbps; assert bytes_to_mbps(39_321_600) == 1.0" exits 0
    - uv run python -c "from metrics import percentile_95; assert percentile_95(list(range(1,289))) == 274" exits 0
    - grep -c "import requests\|import pandas\|from requests\|from pandas" metrics.py returns 0 (no third-party imports)
  </acceptance_criteria>
  <done>Three pure functions with full test coverage. Month ranges use UTC milliseconds and exclude the current partial month. bytes_to_mbps uses the exact formula bytes * 8 / 1_048_576 / 300. percentile_95 uses math.ceil(count * 0.95) for 1-indexed position selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Link aggregation and edge-month metrics pipeline</name>
  <files>metrics.py, tests/test_metrics.py</files>
  <read_first>
    - metrics.py (the three functions created in Task 1 -- their exact signatures, return types, and docstring style)
    - tests/test_metrics.py (the test patterns and import structure from Task 1)
    - .planning/REQUIREMENTS.md (METR-03 link aggregation, CALC-01 daily p95, CALC-02 monthly p95)
  </read_first>
  <behavior>
    - aggregate_link_samples with single link [{"series": [{"bytesTx": 100, "bytesRx": 200}]}] returns [{"tx_bytes": 100, "rx_bytes": 200}]
    - aggregate_link_samples with two links [{"series": [{"bytesTx": 100, "bytesRx": 200}]}, {"series": [{"bytesTx": 50, "bytesRx": 60}]}] returns [{"tx_bytes": 150, "rx_bytes": 260}]
    - aggregate_link_samples with empty list [] returns []
    - aggregate_link_samples with a link whose series is empty [{"series": []}] returns []
    - aggregate_link_samples handles missing bytesTx/bytesRx keys by defaulting to 0
    - compute_edge_month_metrics with empty link_series_result [] returns {"monthly_tx_95th_mbps": 0.0, "monthly_rx_95th_mbps": 0.0, "monthly_total_95th_mbps": 0.0}
    - compute_edge_month_metrics with uniform 1-day data (288 identical samples, single link, bytesTx=1_048_576, bytesRx=2_097_152) returns monthly_tx_95th_mbps approximately equal to 8/300, monthly_rx_95th_mbps approximately equal to 16/300, monthly_total_95th_mbps approximately equal to 24/300
    - compute_edge_month_metrics groups samples by UTC day: sample at index i has timestamp start_ms + (i * 300_000) milliseconds
    - total_mbps per sample is bytes_to_mbps(tx_bytes + rx_bytes), computed from raw bytes before conversion (not sum of converted values)
  </behavior>
  <action>
    RED: Append test functions to tests/test_metrics.py. Import aggregate_link_samples and compute_edge_month_metrics from metrics. For the uniform-data test, construct link_series_result as [{"series": [{"bytesTx": 1_048_576, "bytesRx": 2_097_152}] * 288}] and use a start_ms corresponding to 2026-07-01T00:00:00 UTC. Use pytest.approx for float comparisons. Run tests -- new tests must fail with ImportError for the two new names.

    GREEN: Add two functions to metrics.py. Add import of collections.defaultdict at the top.

    Function 4 -- aggregate_link_samples(link_series_result: list[dict]) -> list[dict]:
    Collect all series arrays from each link dict via link.get("series", []). Find the maximum series length across all links (if no series data, return []). For each sample index from 0 to max_length-1, sum bytesTx (via .get("bytesTx", 0)) and bytesRx (via .get("bytesRx", 0)) across all links that have a sample at that index. Build output list of dicts with keys tx_bytes and rx_bytes.

    Function 5 -- compute_edge_month_metrics(link_series_result: list[dict], start_ms: int) -> dict:
    Step 1: Call aggregate_link_samples(link_series_result).
    Step 2: If empty, return the zero-value dict with keys monthly_tx_95th_mbps, monthly_rx_95th_mbps, monthly_total_95th_mbps all set to 0.0.
    Step 3: For each sample at index i, compute tx_mbps = bytes_to_mbps(sample["tx_bytes"]), rx_mbps = bytes_to_mbps(sample["rx_bytes"]), total_mbps = bytes_to_mbps(sample["tx_bytes"] + sample["rx_bytes"]). Compute sample_date = datetime.fromtimestamp((start_ms + i * 300_000) / 1000, tz=timezone.utc).date().
    Step 4: Group into daily buckets using defaultdict(lambda: {"tx": [], "rx": [], "total": []}). Append tx_mbps, rx_mbps, total_mbps to the corresponding day's lists.
    Step 5: For each day, compute daily_tx_p95 = percentile_95(day["tx"]), daily_rx_p95 = percentile_95(day["rx"]), daily_total_p95 = percentile_95(day["total"]).
    Step 6: Collect daily p95 values into three lists: all_daily_tx, all_daily_rx, all_daily_total.
    Step 7: Return {"monthly_tx_95th_mbps": percentile_95(all_daily_tx), "monthly_rx_95th_mbps": percentile_95(all_daily_rx), "monthly_total_95th_mbps": percentile_95(all_daily_total)}.

    Both functions need Google-style docstrings and type hints.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metrics.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - metrics.py now exports 5 functions: get_target_months, bytes_to_mbps, percentile_95, aggregate_link_samples, compute_edge_month_metrics
    - tests/test_metrics.py has at least 15 test functions total
    - uv run pytest tests/test_metrics.py -v exits with code 0 (all tests pass)
    - uv run python -c "from metrics import aggregate_link_samples; r = aggregate_link_samples([]); assert r == []" exits 0
    - uv run python -c "from metrics import aggregate_link_samples; r = aggregate_link_samples([{'series': [{'bytesTx': 100, 'bytesRx': 200}]}, {'series': [{'bytesTx': 50, 'bytesRx': 60}]}]); assert r == [{'tx_bytes': 150, 'rx_bytes': 260}]" exits 0
    - uv run python -c "from metrics import compute_edge_month_metrics; r = compute_edge_month_metrics([], 0); assert r == {'monthly_tx_95th_mbps': 0.0, 'monthly_rx_95th_mbps': 0.0, 'monthly_total_95th_mbps': 0.0}" exits 0
    - grep -c "from collections import defaultdict\|collections.defaultdict" metrics.py returns at least 1
    - metrics.py imports only stdlib modules (math, calendar, datetime, collections)
  </acceptance_criteria>
  <done>Full metrics computation module with 5 functions. aggregate_link_samples sums bytesTx/bytesRx across all links per sample index. compute_edge_month_metrics runs the full pipeline: aggregate, convert to Mbps, group by UTC day, compute daily p95, compute monthly p95 from daily values. All functions tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Input data to metrics functions | Data originates from VCO API (consumed via Plan 04-02); these pure functions receive it as dicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | aggregate_link_samples | mitigate | Use .get("bytesTx", 0) and .get("bytesRx", 0) for missing keys; return [] for empty input |
| T-04-02 | Tampering | percentile_95 | mitigate | Raise ValueError on empty input rather than allowing IndexError |
| T-04-SC | Tampering | No package installs | accept | No new packages -- all stdlib imports (math, calendar, datetime, collections) |
</threat_model>

<verification>
- All 5 functions importable: `uv run python -c "from metrics import get_target_months, bytes_to_mbps, percentile_95, aggregate_link_samples, compute_edge_month_metrics; print('OK')"`
- Full test suite passes: `uv run pytest tests/ -v`
- No third-party imports: `grep -v '^#' metrics.py | grep -c "import requests\|import pandas\|from requests\|from pandas"` returns 0
</verification>

<success_criteria>
metrics.py exists with 5 pure functions. All 15+ tests pass. Month ranges exclude current month and use UTC milliseconds. bytes_to_mbps uses bytes * 8 / 1_048_576 / 300. Percentile uses ceil(count * 0.95). Aggregation sums across links. Pipeline produces three named metrics per edge per month.
</success_criteria>

<output>
Create `.planning/phases/04-metrics-collection-calculation/04-01-SUMMARY.md` when done
</output>
