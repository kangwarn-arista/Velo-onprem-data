---
phase: 03-cli-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vco_edge_export.py
  - tests/test_cli.py
  - pyproject.toml
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04

must_haves:
  truths:
    - "User can run vco_edge_export.py --collect_95th and the flag is recognized without error"
    - "User can run vco_edge_export.py --months 3 and the value is parsed as integer 3"
    - "Omitting --months defaults to 1"
    - "Running without --collect_95th produces identical CSV output to v1.0 behavior"
    - "Running with --help displays --collect_95th and --months with descriptions"
  artifacts:
    - path: "vco_edge_export.py"
      provides: "argparse CLI integration with --collect_95th and --months flags"
      contains: "build_parser"
    - path: "tests/test_cli.py"
      provides: "Automated tests for CLI argument parsing"
      contains: "test_"
  key_links:
    - from: "vco_edge_export.py:build_parser"
      to: "vco_edge_export.py:__main__"
      via: "build_parser().parse_args() called at start of __main__"
      pattern: "args = build_parser\\(\\)\\.parse_args\\(\\)"
    - from: "tests/test_cli.py"
      to: "vco_edge_export.py:build_parser"
      via: "import and invoke build_parser for isolated testing"
      pattern: "from vco_edge_export import build_parser"
---

<objective>
Add argparse-based CLI argument parsing to vco_edge_export.py with two new flags: --collect_95th (boolean toggle for 95th percentile collection) and --months N (integer, default 1). Existing behavior is preserved when no flags are passed.

Purpose: Phase 3 gates all subsequent 95th percentile work (Phases 4-5). Users need CLI controls before metrics collection and output can be wired in.

Output: Modified vco_edge_export.py with argparse integration, plus automated tests confirming flag behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vco_edge_export.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add build_parser function and argparse integration</name>
  <files>vco_edge_export.py, pyproject.toml</files>
  <read_first>
    - vco_edge_export.py (current full file — understand module-level globals, __main__ block structure, import ordering)
    - pyproject.toml (current dependency groups — need to add pytest to dev group)
  </read_first>
  <behavior>
    - build_parser() returns an ArgumentParser with --collect_95th and --months flags
    - parse_args([]) produces collect_95th=False, months=1
    - parse_args(["--collect_95th"]) produces collect_95th=True, months=1
    - parse_args(["--months", "3"]) produces collect_95th=False, months=3
    - parse_args(["--collect_95th", "--months", "6"]) produces collect_95th=True, months=6
    - parse_args(["--months", "abc"]) raises SystemExit (argparse type=int validation)
    - parse_args(["--months", "0"]) is accepted by argparse (validation of minimum value handled in __main__)
  </behavior>
  <action>
    Add import argparse and import logging at the top of vco_edge_export.py alongside the existing stdlib imports (io, json, time).

    Create a build_parser() function with return type annotation -> argparse.ArgumentParser (placed after normalize_token, before the if __name__ block). Add a Google-style docstring: """Build CLI argument parser for VCO edge export.\n\nReturns:\n    argparse.ArgumentParser: Configured parser with --collect_95th and --months flags.""" The parser description should be "Export VCO edge and license data, with optional 95th percentile bandwidth metrics." Add two arguments:
    - --collect_95th: action="store_true", default=False, help text explaining it enables 95th percentile bandwidth metrics collection
    - --months: type=int, default=1, help text explaining it sets the number of complete months to collect (used with --collect_95th)

    In the __main__ block, insert args = build_parser().parse_args() as the very first line (before the env var validation). After env var validation and token normalization, add a conditional block: if args.months less than 1, use logging.error() to log the error message and exit(1). Then use logging.info() to log a status line indicating whether 95th percentile collection is enabled (and if so, for how many months). The rest of the __main__ block (enterprise fetch, license CSV, merge, output) remains completely unchanged.

    Add pytest as a dev dependency using: uv add --dev "pytest>=8.0" (do NOT edit pyproject.toml directly).
  </action>
  <verify>
    <automated>uv run python -c "from vco_edge_export import build_parser; p = build_parser(); a = p.parse_args([]); assert a.collect_95th == False; assert a.months == 1; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - vco_edge_export.py contains "import argparse" and "import logging" in the imports section
    - vco_edge_export.py contains a function def build_parser() -> argparse.ArgumentParser with a Google-style docstring
    - build_parser() configures --collect_95th with action="store_true" and default=False
    - build_parser() configures --months with type=int and default=1
    - __main__ block starts with args = build_parser().parse_args()
    - __main__ block validates args.months >= 1 and exits with logging.error() if less than 1
    - __main__ block uses logging.info() to log whether 95th percentile collection is active
    - All existing __main__ logic (env validation, API calls, merge, CSV output) is unchanged
    - pyproject.toml [dependency-groups] dev list includes pytest>=8.0 (installed via uv add --dev)
    - uv run python vco_edge_export.py --help exits 0 and output contains --collect_95th and --months
  </acceptance_criteria>
  <done>build_parser() exists and returns a correctly configured ArgumentParser. __main__ parses CLI args, validates --months >= 1, and prints collection mode. Existing behavior is untouched when no flags are passed. pytest is available as a dev dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CLI parser tests</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - vco_edge_export.py (read build_parser function signature and the module-level globals that execute on import: load_dotenv, os.getenv for VCO_TOKEN and VCO_URL)
  </read_first>
  <behavior>
    - test_default_values: parse_args([]) yields collect_95th=False, months=1
    - test_collect_95th_flag: parse_args(["--collect_95th"]) yields collect_95th=True
    - test_months_custom: parse_args(["--months", "3"]) yields months=3
    - test_combined_flags: parse_args(["--collect_95th", "--months", "6"]) yields collect_95th=True, months=6
    - test_months_invalid_type: parse_args(["--months", "abc"]) raises SystemExit
    - test_help_contains_flags: parser.format_help() contains both "--collect_95th" and "--months"
  </behavior>
  <action>
    Create the tests/ directory and an empty tests/__init__.py file.

    Create tests/test_cli.py. At the top, before any imports from the project, set os.environ["VCO_TOKEN"] and os.environ["VCO_URL"] to dummy values ("Token test" and "https://test.example.com/portal/" respectively) using os.environ.setdefault so the module-level load_dotenv and os.getenv calls in vco_edge_export.py do not fail or use real credentials. Then import build_parser from vco_edge_export.

    Write pytest test functions covering all behaviors listed above. Each test creates a fresh parser via build_parser() and calls parse_args() with the appropriate argv list. For the invalid type test, use pytest.raises(SystemExit). For the help text test, call parser.format_help() and assert the substrings "--collect_95th" and "--months" are present.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/__init__.py exists (empty file)
    - tests/test_cli.py exists and contains at least 6 test functions prefixed with test_
    - tests/test_cli.py sets VCO_TOKEN and VCO_URL env vars before importing from vco_edge_export
    - tests/test_cli.py imports build_parser from vco_edge_export
    - uv run pytest tests/test_cli.py -v exits 0 with all tests passing
    - Tests cover: default values, --collect_95th flag, --months with custom value, combined flags, invalid --months type, help text content
  </acceptance_criteria>
  <done>All 6+ pytest tests pass, confirming argparse flags work correctly in isolation without requiring VCO API access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input -> argparse | User-supplied command-line arguments parsed by argparse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | --months argument | mitigate | argparse type=int rejects non-integer input; __main__ validates months >= 1 and exits on invalid values |
| T-03-02 | Denial of Service | --months with very large N | accept | Downstream phases will iterate N months of API calls; large N is a legitimate user choice, not an attack vector in a local CLI tool |
| T-03-SC | Tampering | pytest dev dependency install | accept | pytest is a well-established package (>50M monthly downloads); dev-only dependency not shipped in production; no pip install tasks in this plan (uv add only) |
</threat_model>

<verification>
1. Run `uv run python vco_edge_export.py --help` and confirm output lists --collect_95th and --months with descriptions
2. Run `uv run pytest tests/test_cli.py -v` and confirm all tests pass
3. Confirm that `uv run python vco_edge_export.py` (no flags) would follow the same code path as before (env validation, API calls, merge, CSV write) -- structurally verified by reading __main__ block
</verification>

<success_criteria>
- vco_edge_export.py accepts --collect_95th and --months N flags via argparse
- Default behavior (no flags) is identical to v1.0
- --months defaults to 1, validates >= 1
- --help shows both new flags
- All automated tests pass
</success_criteria>

<output>
Create `.planning/phases/03-cli-configuration/03-01-SUMMARY.md` when done
</output>
