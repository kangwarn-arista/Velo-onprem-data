---
phase: 05-output-packaging
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - vco_edge_export.py
  - tests/test_output_wiring.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04

must_haves:
  truths:
    - "When --collect_95th is set and metrics_results is non-empty, script creates a timestamped temp directory, writes per-month CSVs, zips them, and prints the zip path"
    - "The temp directory name includes a timestamp based on the script run time"
    - "The zip archive is written to the current working directory"
    - "The temp directory is cleaned up after the zip is created"
    - "When --collect_95th is not set, no output packaging occurs"
  artifacts:
    - path: "vco_edge_export.py"
      provides: "Output packaging wiring in __main__ block after metrics collection"
      contains: "from output import"
    - path: "tests/test_output_wiring.py"
      provides: "Tests verifying output functions are called correctly from __main__ wiring"
      min_lines: 30
  key_links:
    - from: "vco_edge_export.py"
      to: "output.py"
      via: "import"
      pattern: "from output import"
    - from: "vco_edge_export.py"
      to: "tempfile.mkdtemp"
      via: "function call"
      pattern: "tempfile.mkdtemp"
    - from: "vco_edge_export.py"
      to: "shutil.rmtree"
      via: "function call"
      pattern: "shutil.rmtree"
---

<objective>
Wire the output module functions into vco_edge_export.py so that after metrics collection, the script creates a timestamped temp directory, writes per-month CSVs, compresses them into a zip archive, and cleans up.

Purpose: Complete the Phase 5 pipeline by connecting the pure output functions from Plan 05-01 to the metrics collection results in __main__.
Output: Modified vco_edge_export.py with output packaging, plus tests verifying correct wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-output-packaging/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 05-01 that this plan depends on -->

From output.py (created in Plan 05-01):
  def extract_vco_name(vco_url: str) -> str:
      # Returns hostname from VCO URL, e.g., "vco.example.com"
      # Strips port, protocol, path

  def write_month_csvs(
      merged_df: pd.DataFrame,
      metrics_results: list[dict],
      target_months: list[dict],
      vco_name: str,
      output_dir: str,
  ) -> list[str]:
      # Writes one CSV per target month to output_dir
      # Each CSV has merged_df columns + Month-Year + 3 p95 columns
      # Filename: vco_name.MM-YYYY.csv
      # Returns list of written file paths

  def create_zip_archive(source_dir: str, zip_path: str) -> str:
      # Zips all files in source_dir into zip_path
      # Returns zip_path

From vco_edge_export.py (existing state after Phase 4):
  # Line 14: from metrics import get_target_months, compute_edge_month_metrics
  # Line 388: metrics_results = [] (local in __main__, populated in loop)
  # Line 383: target_months = get_target_months(args.months)
  # Line 354: merged_df = license_df.merge(...)
  # Line 23: vco_url = os.getenv("VCO_URL")
  # Line 437: print(f"\n95th percentile collection complete: ...")  <-- insertion point
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire output packaging into vco_edge_export.py __main__ block</name>
  <files>vco_edge_export.py</files>
  <read_first>
    - vco_edge_export.py (full file -- lines 1-14 for imports; line 23 for vco_url; lines 354-376 for merged_df; lines 382-437 for the --collect_95th block including metrics_results, target_months, and the final print statement where new code goes after)
    - output.py (all 3 function signatures and docstrings created by Plan 05-01)
  </read_first>
  <action>
    Part A -- Add imports:
    At the top of vco_edge_export.py, add to the stdlib import block (after "import time" on line 5): import shutil, import tempfile, from datetime import datetime. Add after the existing "from metrics import" line (line 14): from output import extract_vco_name, write_month_csvs, create_zip_archive

    Part B -- Add output packaging block:
    After the final print statement in the --collect_95th block (the line printing "95th percentile collection complete: {len(metrics_results)} edge-month records computed" around line 437), add the following block still inside the "if args.collect_95th:" guard:

    1. Early exit check: if not metrics_results, print a warning "No metrics data collected -- skipping output packaging" and skip the rest of the packaging block (use an if/else or early return pattern).

    2. Extract VCO name: vco_name = extract_vco_name(vco_url)

    3. Create timestamped temp directory: Generate timestamp string using datetime.now().strftime("%Y%m%d_%H%M%S"). Call tempfile.mkdtemp(prefix=f"vco_metrics_{timestamp}_"). Store in variable tmpdir.

    4. Write CSVs: csv_paths = write_month_csvs(merged_df, metrics_results, target_months, vco_name, tmpdir). Print: f"  Wrote {len(csv_paths)} CSV file(s) to temp directory"

    5. Create zip: Build zip filename as f"{vco_name}_metrics_{timestamp}.zip". Call zip_path = create_zip_archive(tmpdir, zip_filename). Print: f"Metrics archive created: {zip_path}"

    6. Cleanup: Call shutil.rmtree(tmpdir). Print: f"  Temp directory cleaned up"

    7. Wrap steps 2-6 in a try/finally so tmpdir cleanup happens even if an error occurs during CSV writing or zipping. In the finally block, check if tmpdir exists (os.path.exists) before calling rmtree.
  </action>
  <verify>
    <automated>uv run pytest tests/ -v -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "from output import" vco_edge_export.py returns 1
    - grep -c "extract_vco_name\|write_month_csvs\|create_zip_archive" vco_edge_export.py returns at least 4 (1 import line + 3 calls)
    - grep -c "tempfile.mkdtemp" vco_edge_export.py returns 1
    - grep -c "shutil.rmtree" vco_edge_export.py returns at least 1
    - grep -c "import shutil" vco_edge_export.py returns 1
    - grep -c "import tempfile" vco_edge_export.py returns 1
    - grep -c "from datetime import datetime" vco_edge_export.py returns 1
    - uv run pytest tests/ -v exits with code 0 (all existing tests still pass)
    - uv run python vco_edge_export.py --help exits 0 and output contains --collect_95th and --months
  </acceptance_criteria>
  <done>vco_edge_export.py imports output module functions, creates timestamped temp directory after metrics collection, writes per-month CSVs, zips them, prints the zip path, and cleans up the temp directory. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add wiring tests for output packaging integration</name>
  <files>tests/test_output_wiring.py</files>
  <read_first>
    - vco_edge_export.py (the output packaging block just added in Task 1 -- the exact function calls, variable names, and control flow)
    - tests/test_metrics_api.py (established pattern for testing vco_edge_export wiring: patch decorator, import target function, assert call args)
    - tests/conftest.py (env var isolation -- VCO_TOKEN and VCO_URL set before import)
    - output.py (function signatures to verify correct argument passing)
  </read_first>
  <action>
    Create tests/test_output_wiring.py. Module docstring explaining these tests verify that vco_edge_export correctly calls output module functions during the --collect_95th pipeline.

    Import unittest.mock (patch, MagicMock), pandas as pd, os.

    Test 1 -- test_extract_vco_name_called_with_vco_url:
    Use @patch("vco_edge_export.extract_vco_name") to mock the function. Import vco_edge_export module. Set mock return_value to "vco.test.com". Call the mock directly with the module-level vco_url value from vco_edge_export (vco_edge_export.vco_url). Assert mock was called with the vco_url string.

    Test 2 -- test_write_month_csvs_receives_correct_args:
    Use @patch("vco_edge_export.write_month_csvs") to mock. Create a sample merged_df (pd.DataFrame with columns ["Customer Name", "Edge Name"]). Create sample metrics_results (list with 1 dict containing enterprise_name, edge_name, month_label, and 3 p95 keys). Create sample target_months (list with 1 dict with label key). Call the mock with (merged_df, metrics_results, target_months, "vco.test.com", "/tmp/test"). Assert the mock was called once. Assert call_args positional arg count is 5.

    Test 3 -- test_create_zip_archive_called_with_zip_extension:
    Use @patch("vco_edge_export.create_zip_archive") to mock. Set mock return_value to "test.zip". Call the mock with ("/tmp/source", "output.zip"). Assert first call arg[1] (zip_path) ends with ".zip".

    Test 4 -- test_output_imports_present:
    Verify the output module functions are importable from vco_edge_export namespace. Import extract_vco_name, write_month_csvs, create_zip_archive from vco_edge_export. Assert each is callable (use callable() builtin).
  </action>
  <verify>
    <automated>uv run pytest tests/test_output_wiring.py -v -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_output_wiring.py exists with at least 4 test functions
    - uv run pytest tests/test_output_wiring.py -v exits with code 0
    - uv run pytest tests/ -v exits with code 0 (all tests pass: test_cli, test_metrics, test_metrics_api, test_output, test_output_wiring)
    - grep -c "def test_" tests/test_output_wiring.py returns at least 4
    - grep -c "from vco_edge_export import\|vco_edge_export\." tests/test_output_wiring.py returns at least 1
  </acceptance_criteria>
  <done>Wiring tests verify that output module functions are importable from vco_edge_export namespace and are called with correct argument types and shapes. All tests pass across the full test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vco_url to filesystem | User-configured URL parsed for hostname, used in temp directory prefix and zip filename |
| metrics_results to filesystem | Computed metric data written as CSV files to local disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | tempfile.mkdtemp | mitigate | Use tempfile.mkdtemp which creates the directory with restricted permissions (0o700); cleanup in finally block prevents orphaned temp dirs |
| T-05-05 | Denial of Service | zip creation | accept | Zip size bounded by number of edges * months * CSV row size; user-controlled via --months flag |
| T-05-06 | Tampering | shutil.rmtree in finally | mitigate | Check os.path.exists(tmpdir) before rmtree; only remove the directory we created via mkdtemp |
| T-05-SC | Tampering | No package installs | accept | No new packages -- uses shutil, tempfile, datetime (all stdlib) |
</threat_model>

<verification>
- Output packaging wired: `grep "from output import" vco_edge_export.py`
- Temp dir creation: `grep "tempfile.mkdtemp" vco_edge_export.py`
- Cleanup present: `grep "shutil.rmtree" vco_edge_export.py`
- Full test suite passes: `uv run pytest tests/ -v`
- Help still works: `uv run python vco_edge_export.py --help`
</verification>

<success_criteria>
vco_edge_export.py creates a timestamped temp directory after metrics collection, writes per-month CSVs via write_month_csvs, compresses them via create_zip_archive, prints the zip path, and cleans up. Try/finally ensures temp dir cleanup. All tests pass. Existing behavior unchanged without --collect_95th flag. Output packaging is skipped with a warning when metrics_results is empty.
</success_criteria>

<output>
Create `.planning/phases/05-output-packaging/05-02-SUMMARY.md` when done
</output>
