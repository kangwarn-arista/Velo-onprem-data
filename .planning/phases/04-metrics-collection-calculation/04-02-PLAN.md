---
phase: 04-metrics-collection-calculation
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - vco_edge_export.py
  - tests/test_metrics_api.py
autonomous: true
requirements:
  - METR-01
  - CALC-03

must_haves:
  truths:
    - "Script calls metrics/getEdgeLinkSeries with enterpriseId, edgeId (numeric id field), interval with start/end in milliseconds, and metrics list containing bytesTx and bytesRx"
    - "When --collect_95th is passed, script computes and stores 3 p95 metrics per edge per month"
    - "When --collect_95th is not passed, existing behavior is completely unchanged"
  artifacts:
    - path: "vco_edge_export.py"
      provides: "API wrapper for getEdgeLinkSeries and metrics collection orchestration in __main__"
      contains: "get_edge_link_series"
    - path: "tests/test_metrics_api.py"
      provides: "Test verifying API call params match METR-01 contract"
      min_lines: 20
  key_links:
    - from: "vco_edge_export.py"
      to: "metrics.py"
      via: "import"
      pattern: "from metrics import"
    - from: "vco_edge_export.py"
      to: "api_call"
      via: "function call in get_edge_link_series"
      pattern: "api_call.*metrics/getEdgeLinkSeries"
---

<objective>
Add the VCO API wrapper for metrics/getEdgeLinkSeries and wire the full metrics collection pipeline into the __main__ block, activated by the --collect_95th flag.

Purpose: Connect the pure computation functions from Plan 04-01 to live VCO API data, completing the Phase 4 metrics collection and calculation pipeline.
Output: Modified vco_edge_export.py with API wrapper and orchestration logic, plus a test verifying the API call contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics-collection-calculation/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 04-01 that this plan depends on -->

From metrics.py (created in Plan 04-01):
  def get_target_months(num_months: int, reference_date: date | None = None) -> list[dict]:
      # Returns list of dicts, each with keys: year (int), month (int), start_ms (int), end_ms (int), label (str "MM-YYYY")
      # Ordered chronologically (oldest first). Never includes the current partial month.

  def compute_edge_month_metrics(link_series_result: list[dict], start_ms: int) -> dict:
      # Input: link_series_result is the "result" array from the getEdgeLinkSeries API response
      # Input: start_ms is the start timestamp of the month interval in milliseconds
      # Returns: {"monthly_tx_95th_mbps": float, "monthly_rx_95th_mbps": float, "monthly_total_95th_mbps": float}
      # Returns all-zeros dict for empty input

From vco_edge_export.py (existing):
  def api_call(method: str, params: dict, max_retries: int = 5) -> dict
      # JSON-RPC 2.0 POST to VCO. Returns parsed response dict or {} on error.
  def build_parser() -> argparse.ArgumentParser
      # --collect_95th (store_true), --months (int, default 1)
  def get_edges(ent: dict) -> dict
      # Returns {"result": {"data": [edge_dicts...]}}
  class VCOAuthError(Exception)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_edge_link_series API wrapper and wire metrics collection into __main__</name>
  <files>vco_edge_export.py, tests/test_metrics_api.py</files>
  <read_first>
    - vco_edge_export.py (full file -- see api_call at lines 33-85 for wrapper pattern, get_edges at lines 104-124 for domain function pattern, get_network_license_export at lines 127-140 for docstring pattern, __main__ edge loop at lines 261-279, imports at lines 1-12, final CSV write at lines 322-326)
    - metrics.py (function signatures and return types created by Plan 04-01)
    - tests/test_cli.py (env var isolation pattern: os.environ lines before import)
    - .planning/REQUIREMENTS.md (METR-01 API call contract, CALC-03 output format)
  </read_first>
  <action>
    Part A -- Add import statement:
    After the existing import block (after "import os" on line 12), add: from metrics import get_target_months, compute_edge_month_metrics

    Part B -- Add get_edge_link_series function:
    Place it after get_network_license_export (after line 140) and before normalize_token. Follow the same docstring and structure pattern as get_network_license_export.

    Signature: get_edge_link_series(enterprise_id: int, edge_id: int, start_ms: int, end_ms: int) -> dict
    Body: method = "metrics/getEdgeLinkSeries". Params dict with 4 keys: "enterpriseId" set to enterprise_id, "edgeId" set to edge_id, "interval" set to {"start": start_ms, "end": end_ms}, "metrics" set to ["bytesTx", "bytesRx"]. Return api_call(method, params).
    Google-style docstring explaining it fetches per-edge link bandwidth series for a given time interval.

    Part C -- Collect edge info during existing edge loop:
    Before the enterprise loop (before line 259 which initializes edge_status_rows), add: edge_info_list = []
    Inside the inner edge loop, after the edge_status_rows.append call (after line 279), add a conditional block: if args.collect_95th, append to edge_info_list a dict with keys "enterprise_id" (ent["id"]), "enterprise_name" (ent["name"]), "edge_id" (edge.get("id")), "edge_name" (edge.get("name", "")).

    Part D -- Add metrics collection block after CSV write:
    After the final print/done line (line 326), add a block guarded by: if args.collect_95th:
    Inside this block:
    1. target_months = get_target_months(args.months)
    2. Print: f"\nCollecting 95th percentile metrics for {len(target_months)} month(s): {', '.join(m['label'] for m in target_months)}"
    3. Initialize: metrics_results = []
    4. Outer loop over edge_info_list with progress: print(f"\n  Processing edge: {edge_info['edge_name']} ({edge_info['enterprise_name']})")
    5. Inner loop over target_months. Call get_edge_link_series(edge_info["enterprise_id"], edge_info["edge_id"], month["start_ms"], month["end_ms"]). Extract link_series = response.get("result", []).
    6. Call metrics = compute_edge_month_metrics(link_series, month["start_ms"]).
    7. Append to metrics_results: dict with keys "enterprise_name" and "edge_name" from edge_info, "month_label" from month["label"], plus all three keys from the metrics dict (use ** unpacking).
    8. Print per-edge-month: f"    {month['label']}: tx={metrics['monthly_tx_95th_mbps']:.4f} rx={metrics['monthly_rx_95th_mbps']:.4f} total={metrics['monthly_total_95th_mbps']:.4f} Mbps"
    9. After both loops, print: f"\n95th percentile collection complete: {len(metrics_results)} edge-month records computed"
    Note: metrics_results remains a local variable in __main__ for now. Phase 5 will consume it for CSV/zip output.

    Part E -- Create tests/test_metrics_api.py:
    Set up env vars before import (same pattern as test_cli.py): os.environ["VCO_TOKEN"] = "Token test", os.environ["VCO_URL"] = "https://test.example.com/portal/"
    Import get_edge_link_series from vco_edge_export. Import unittest.mock.patch.

    Test 1 -- test_get_edge_link_series_params: Use @patch("vco_edge_export.api_call") decorator. Call get_edge_link_series(100, 200, 1000000, 2000000). Assert mock was called once. Assert the call args: first positional arg is "metrics/getEdgeLinkSeries", second positional arg is {"enterpriseId": 100, "edgeId": 200, "interval": {"start": 1000000, "end": 2000000}, "metrics": ["bytesTx", "bytesRx"]}.

    Test 2 -- test_get_edge_link_series_returns_api_result: Use @patch("vco_edge_export.api_call"). Set mock return_value to {"result": [{"series": []}]}. Call get_edge_link_series(1, 2, 0, 1000). Assert the return value equals {"result": [{"series": []}]}.
  </action>
  <verify>
    <automated>uv run pytest tests/ -v -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def get_edge_link_series" vco_edge_export.py returns 1
    - grep -c "metrics/getEdgeLinkSeries" vco_edge_export.py returns at least 1
    - grep -c "from metrics import" vco_edge_export.py returns 1
    - grep -c "compute_edge_month_metrics" vco_edge_export.py returns at least 1
    - grep -c "metrics_results" vco_edge_export.py returns at least 2 (initialization and append)
    - grep -c "edge_info_list" vco_edge_export.py returns at least 3 (init, append, iterate)
    - tests/test_metrics_api.py exists with exactly 2 test functions
    - uv run pytest tests/ -v exits with code 0 (all tests pass: test_cli.py, test_metrics.py, test_metrics_api.py)
    - uv run python vco_edge_export.py --help exits 0 and output contains --collect_95th and --months
    - grep -c '"enterpriseId"' vco_edge_export.py in get_edge_link_series function confirms the param key matches VCO API contract
    - grep -c '"edgeId"' vco_edge_export.py in get_edge_link_series function confirms edge id param
  </acceptance_criteria>
  <done>get_edge_link_series wraps api_call with method "metrics/getEdgeLinkSeries" and params including enterpriseId, edgeId, interval (start/end in ms), and metrics list. __main__ collects edge IDs during the edge loop, then when --collect_95th is set, iterates all edges and target months to fetch link series, compute p95 metrics, and store results. All existing and new tests pass. Existing behavior unchanged without the flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VCO API response to script | getEdgeLinkSeries response is untrusted external data parsed into dicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | get_edge_link_series | mitigate | Response parsed with .get("result", []) default; downstream compute_edge_month_metrics handles empty input returning zeroed metrics |
| T-04-04 | Denial of Service | metrics collection loop | accept | Large --months * many edges = many API calls; user-controlled via CLI flag, acceptable per project constraints |
| T-04-SC | Tampering | No package installs | accept | No new packages in this plan |
</threat_model>

<verification>
- All tests pass: `uv run pytest tests/ -v`
- API wrapper exists: `uv run python -c "from vco_edge_export import get_edge_link_series; print('OK')"`
- Help shows flags: `uv run python vco_edge_export.py --help`
- Metrics import present: `grep "from metrics import" vco_edge_export.py`
</verification>

<success_criteria>
get_edge_link_series function calls api_call with "metrics/getEdgeLinkSeries" and correct params per METR-01. __main__ block collects edge numeric IDs during the edge loop, then when --collect_95th is set, iterates edges and months to compute and store 3 p95 metrics per edge per month per CALC-03. All tests pass. Existing behavior unchanged without the flag.
</success_criteria>

<output>
Create `.planning/phases/04-metrics-collection-calculation/04-02-SUMMARY.md` when done
</output>
